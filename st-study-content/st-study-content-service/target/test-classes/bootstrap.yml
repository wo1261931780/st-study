#这里只需要进行单元测试，所以不需要启动 web 服务
#微服务配置
spring:
  application:
    name: content-service-test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/study_database?serverTimezone=UTC&userUnicode=true&useSSL=false&
    username: root
    password: junw
# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml
